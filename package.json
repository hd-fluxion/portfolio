{
  "name": "portfolio-monorepo",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev:site": "npm --workspace apps/site run dev",
    "dev": "npm --workspace apps/site run dev"
  }
}
